<?php

class Sender {
    public static function sendMail($user, $subject, $body) {
//        Mail::send('emails.confirm', ['body' => $body], function ($message) use ($user, $subject) {
//            $message->to([$user->email => $user->username])
//                ->subject($subject);
//        });
    }
} 